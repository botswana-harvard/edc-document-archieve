class GalleryItem {
  GalleryItem({
    required this.id,
    required this.imageUrl,
  });
// id image (image url) to use in hero animation
  final String id;
  // image url
  final String imageUrl;
}
